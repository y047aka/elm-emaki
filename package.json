{
  "name": "elm-emaki",
  "version": "0.0.1",
  "private": true,
  "description": "elm-emaki",
  "bin": {
    "elm-emaki": "cli/main.ts"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "example": "cd example && run-pty % elm-watch hot % npm run esbuild -- --serve=9000 --servedir=example/public",
    "esbuild": "esbuild example/app.ts --bundle --outdir=example/public/build --public-path=example/build/",
    "prettier": "prettier --write  **/*.{ts,md,yml}",
    "prettier-check": "prettier --check **/*.{ts,md,yml}"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "elm": "^0.19.1-5",
    "elm-format": "^0.8.7",
    "elm-review": "^2.10.3",
    "elm-watch": "^1.2.0-beta.4",
    "esbuild": "^0.19.4",
    "prettier": "^2.8.8",
    "run-pty": "^4.1.0",
    "typescript": "^5.2.2"
  }
}
